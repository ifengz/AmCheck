# AmReview UI 优化完成说明

> 2026-08-15 · 优化当前体验 + 为产品链接检测扩展留空间

## 已完成的优化

### 1. 视觉优化 ✅

**状态徽章系统**
- 登录状态从 `🟢3天前` 升级为色块徽章
- 🟢 在线(绿) / ⚪ 离线(灰) / 🟡 过期(黄)
- 统一圆角设计，视觉层级清晰

**数据卡片增强**
- Metric 卡片显示百分比占比（如 `✅ 正常 15 +75%`）
- hover 阴影效果，交互反馈更好
- 多站点时自动显示分站存活率对比

**全局样式**
- 表格圆角 8px，容器圆角 10px
- 空值统一显示 `—`（破折号）
- 色彩保持 Amazon 橙主题

### 2. 交互优化 ✅

**进度显示**
- 增加域名简称 + 状态 + 标题预览
- 格式：`[3/20] com · R1XXX → ✅ 正常 · Great product…`

**登录按钮**
- 动态类型：全部在线 → primary(橙)，部分在线 → secondary(灰)
- 状态数 `{online}/{total}` 直观显示

**CSV 导出**
- 文件名自动加时间戳 + 条数：`amreview_20260815_1200_20条.csv`

**状态变化标记**
- 历史对比时，状态变化显示 ⚠️ 符号

### 3. 扩展预留（已设计未启用）🔮

**产品链接检测框架**
```python
# engine.py 已预留
@dataclass
class ProductRef:
    asin, domain, url
    # 检测字段：title, price, deal_tag, sold_by, rating, 
    # review_count, main_image, availability

def check_products(self, product_refs) -> list[dict]:
    """提取产品页所有关键字段"""
    pass
```

**数据库表预留**
```sql
-- app.py 已预留
CREATE TABLE product_history (
    asin, domain, title, price, currency, deal_tag,
    sold_by, rating, review_count, availability, ...
)
```

**UI 切换器预留**
- 检测类型选择：评价链接 / 产品链接(ASIN)
- AI 复核按钮：未知项截图分析（GLM-4V）
- 批量队列按钮：100+ 条后台运行
- 状态筛选器：多选框快速定位
- 快速开始引导：首次使用教程

## 组件使用（遵守不手搓原则）

✅ 使用的公共组件：
- `st.container(border=True)` — 卡片
- `st.metric()` — 统计数值
- `@st.dialog()` — 弹窗
- `st.columns()` — 响应式布局
- `st.expander()` — 折叠面板
- `st.dataframe()` — 表格

✅ 轻量样式定制：
- CSS 变量（颜色、圆角、hover）
- 状态徽章 inline HTML

❌ 没有手搓：
- 自定义表格组件
- 完整的前端框架
- 复杂 JavaScript 逻辑

## 文件变更清单

| 文件 | 变更内容 | 行数变化 |
|------|----------|---------|
| `code/app.py` | 状态徽章、进度优化、分站统计、扩展预留 | +80 行 |
| `code/engine.py` | ProductRef 类预留、check_products 方法预留 | +35 行 |
| `doc/UI优化与扩展设计.md` | 详细设计文档 | 新增 |

## 下一步建议

### 立即可做（用户已明确需求）

1. **实现产品链接检测** — 框架已就绪，只需补充选择器和字段提取逻辑
   - 标题：`#productTitle`
   - 价格：`.a-price .a-offscreen`
   - 评分：`#acrPopover[title]`
   - Deal Tag：`.dealBadge`
   - Sold by：`#sellerProfileTriggerId`
   - 主图：`#landingImage[src]`

2. **启用检测类型切换器** — 取消注释 [app.py:274-285](app.py:274-285)

3. **数据库迁移** — 取消注释 [app.py:58-64](app.py:58-64) 创建 `product_history` 表

### 可选增强

4. **AI 复核未知项** — 集成 GLM-4V API，分析 `screenshots/` 下的截图
5. **批量队列模式** — SQLite 任务表 + 后台进程轮询
6. **状态筛选器** — 取消注释 [app.py:375-377](app.py:375-377)

## 测试建议

启动服务验证优化效果：
```bash
cd code
.venv/bin/streamlit run app.py
```

检查项：
- ✅ 登录弹窗的状态徽章显示正常
- ✅ 顶栏登录按钮根据状态变色
- ✅ 检测进度显示域名简称
- ✅ 结果卡片显示百分比
- ✅ 多站点时显示分站统计
- ✅ CSV 文件名包含时间戳和条数

## 设计原则回顾

1. **组件优先** — 不手搓，用 Streamlit 公共组件
2. **预留接口** — 扩展提前设计，注释清晰
3. **渐进增强** — 核心功能稳定后再加高级功能
4. **保持克制** — 每个功能对应真实需求
5. **数据驱动** — 基于历史数据的洞察优先

---

**优化完成** ✅  
当前代码已通过语法检查，可直接运行。扩展功能框架已就绪，随时可启用。
